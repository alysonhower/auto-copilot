[project]
name = "auto-copilot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydoll-python>=2.15.1",
    "openpyxl>=3.1.0",
    "click>=8.3.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
